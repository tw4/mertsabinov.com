import BlogList from "@/components/blogList/BlogList";
import Layout from "@/components/layout/Layout";

const Blog = () => {
  return (
    <Layout>
      <BlogList />
    </Layout>
  );
};

export default Blog;
